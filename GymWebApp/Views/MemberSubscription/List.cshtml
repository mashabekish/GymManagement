@using GymDomain.Entities;

@model List<MemberSubscription>

<h2>Member Subscriptions List</h2>

<body>
    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card h-100 w-100">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">Add New Member Subscription</h5>
                    <p class="card-text">Click here to add a new member subscription.</p>
                    <a href="@Url.Action("Create", "MemberSubscription")" class="btn btn-success mt-auto">Add Member Subscription</a>
                </div>
            </div>
        </div>

        @foreach (var subscription in Model)
        {
            @await Html.PartialAsync("_MemberSubscriptionDetailsCard", subscription)
        }
    </div>
</body>
